<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <!-- <record id="product_price_action" model="ir.ui.view">
            <field name="name">product.price</field>
            <field name="model">product.template</field>
            <field name="arch" type="xml">
                <form>
                   <p>Flag</p>
                </form>
            </field>
        </record>

        <act_window name="Product Price"
            id="product_price_action"
            res_model="product.template"
            binding_model="product.product_template_search_view"
            binding_views="form"
            view_mode="form"
            target="new"
        /> -->

        
        <record id="launch_the_wizard" model="ir.actions.act_window">
            <field name="name">Launch the Wizard</field>
            <field name="res_model">product.template</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="binding_model_id" ref="product.product_template_search_view"/>
        </record>
</odoo>



